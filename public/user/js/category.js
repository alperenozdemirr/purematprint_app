/**
 * category.js — Kategori listesi (category.html)
 *
 * NE YAPAR:
 *   Sayfa özel init (gerekirse genişletilir).
 *   Sıralama ve sayfalama index5.js tarafından yapılır.
 *
 * NE YAPMAZ:
 *   Kategori filtreleme (products.html'de index5.js yapar).
 */

document.addEventListener('DOMContentLoaded', () => {
  // Sıralama: index5.js → initProductSort()
  // Sayfalama: index5.js → initProductGridPagination()
});
